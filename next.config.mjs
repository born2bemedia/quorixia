/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['localhost', 'quorixia-cms.onrender.com', 'cms.quorixia.com'],
    },
  };
  
  export default nextConfig;
  